<?xml version="1.0" encoding="utf-8"?>
<!--
    Input Method descriptor.
    Defines the subtypes (language/keyboard modes) that SmartKeyboard supports.
    Each subtype maps to a locale and a keyboard layout mode.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:supportsSwitchingToNextInputMethod="true">

    <!-- English (US) – QWERTY layout -->
    <subtype
        android:imeSubtypeExtraValue="KeyboardLayoutSet=qwerty"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true"
        android:label="@string/subtype_en_us" />

    <!-- English (GB) – QWERTY layout -->
    <subtype
        android:imeSubtypeExtraValue="KeyboardLayoutSet=qwerty"
        android:imeSubtypeLocale="en_GB"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true"
        android:label="@string/subtype_en_gb" />

</input-method>
